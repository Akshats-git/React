# React
two options - 
npx create-react-app
npm create vite@latest

App.jsx can return only 1 element so to return multiple elements, we can wrap them up around div tags. But after a time, it got changed to just <></>

The exported function names need to be in capital in vite.